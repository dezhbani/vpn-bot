import React from 'react';
import Sidebar from '../public/Sidebar';

const HomePage = () => {
    return (
        <div>
            <Sidebar />
            <h1>home</h1>
        </div>
    );
};

export default HomePage;